[package]
name = "memra-runtime"
version = "0.0.0"
edition = "2024"

[dependencies]
memra-gguf = { path = "../memra-gguf" }
cudarc = { version = "0.19", default-features = false, features = ["std", "cuda-13010", "dynamic-loading", "driver", "cublas", "cublaslt", "nvrtc"] }

[[bin]]
name = "gemm-check"
path = "src/bin/gemm_check.rs"
